extends layout

block content
  .container
    h1= message
    p #{title}. Enter your name:

    form.form(action='post')
      input(
        type='text'
        name='name'
        placeholder='Bob Marley'
      )
      button.button(type='submit') Submit